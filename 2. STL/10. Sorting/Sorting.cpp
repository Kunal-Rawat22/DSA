#include<bits/stdc++.h>

using namespace std;

int main()
{
    // sort(a, a + n);
    // sort(v.begin(), v.end());
    // sort(a + 2, a + 4);      from 2nd index to 3rd index
    // pair<int, int> a[] = {{1, 2}, {2, 1}, {4, 1}};

    //sort according to the second element of the pair
    //if the second elements of the pairs are same , then sort
    //it according to the first element in descending

    //sort(a, a+n, comp)
    //{{4,1},{2,1},{1,2}}

    return 0;
}